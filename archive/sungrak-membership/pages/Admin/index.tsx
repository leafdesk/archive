import { Button, Container } from '@mui/material';
import { useRouter } from 'next/router';

const Page = () => {
  const router = useRouter();

  return (
    <>
      <Container></Container>
    </>
  );
};

export default Page;
