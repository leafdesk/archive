import styled from '@emotion/native';

Date.V1 = styled.Text`
  font-size: 14px;
  font-weight: 400;
  padding: 8px 0 0;
  color: #a0a0a0;
`;

export default function Date({ children }) {
  return <>{children}</>;
}
